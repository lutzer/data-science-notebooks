# WLA Dashboard

Visualizes the data generated by the WLA notebooks using react + radix ui + Recharts.

## Development

Run dev server with `npm install`then `npm run dev`.

## Deployment

via github actions or `npm run build`. APp gets build into the */dist* folder


